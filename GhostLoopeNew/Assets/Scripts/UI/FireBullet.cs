using System.Collections;
using System.Collections.Generic;
using UnityEngine;


public class FireBullet : SpecialBullet
{
    // Start is called before the first frame update
    void Start()
    {
        damage = 20.0f;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
